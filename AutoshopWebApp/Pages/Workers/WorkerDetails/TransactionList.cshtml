@page "{id:int?}"
@model AutoshopWebApp.Pages.Workers.WorkerDetails.TransactionListModel

@{
    ViewData["Title"] = "Сведения о переводах";
}

<h2>Сведения о переводах</h2>

<p>
    <a class="btn btn-default" asp-page="AddTransaction" asp-route-id="@Model.WorkerCrossPageData.WorkerID">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OutputData.TransactionOrders[0].TransactionOrder.OrderNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OutputData.TransactionOrders[0].Position.PositionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OutputData.TransactionOrders[0].TransactionOrder.OrderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OutputData.TransactionOrders[0].TransactionOrder.Reason)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.OutputData.TransactionOrders)
        {
            <tr>
                <td>
                    @Html.DisplayFor(model => item.TransactionOrder.OrderNumber)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Position.PositionName)
                </td>
                <td>
                    @Html.DisplayFor(model => item.TransactionOrder.OrderDate)
                </td>
                <td>
                    @Html.DisplayFor(model => item.TransactionOrder.Reason)
                </td>
            </tr>
        }
    </tbody>
</table>
